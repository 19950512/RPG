#!/bin/bash
# Configuração para ambiente de produção
# Copie este arquivo para prod.env e ajuste conforme necessário

# Porta gRPC para produção
export GRPC_PORT=5008

# Outras configurações de produção
export GAME_ENV=production
export DEBUG=false

echo "🏭 Configuração de PRODUÇÃO carregada:"
echo "   - GRPC_PORT: $GRPC_PORT"
echo "   - GAME_ENV: $GAME_ENV"
echo "   - DEBUG: $DEBUG"
echo ""
echo "💡 Use: source prod.env antes de rodar o cliente"
echo "   Exemplo: source prod.env && cd src && python -m GameClient.main"
