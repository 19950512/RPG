using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.Hosting;

namespace GameServer.Services;

// Warmup service removido porque o seed ocorre via migrations e o WorldEntityManager carrega entidades por conta própria.
// Arquivo mantido apenas como placeholder para evitar referências perdidas.
// (Intencionalmente vazio)
